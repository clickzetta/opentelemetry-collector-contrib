type: apikeyrouting
display_name: API Key Routing Connector

status:
  class: connector
  stability:
    alpha: [traces_to_traces, metrics_to_metrics, logs_to_logs]
  distributions: [contrib]
  codeowners:
    active: []

tests:
  skip_lifecycle: true
  skip_shutdown: true
